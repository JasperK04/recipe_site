services:
  recipes:
    build: .
    container_name: recipes
    networks:
      - jasper
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/recipes.db
    volumes:
      - ./data:/app/data

networks:
  jasper:
    external: true